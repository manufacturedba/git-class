section
    h5 Q & A
    code(data-trim)
        div(id="qa", contentEditable="true", style="font-size:0.5em;background:#3F3F3F;color: #DCDCDC;width:100%;height:auto;padding:15px;text-align:left")
    button(id="highlight", onclick="highlight()") Update Highlighting
    style.
        #highlight {
            background: #3F3F3F;
            padding: 10px;
            color: #DCDCDC;
            border: none;
            border-radius: 3px;
            text-transform: uppercase;
        }
        #highlight:focus {
            color: #ccc;
            outline: none;
        }
    script(type="text/javascript").
        function highlight() {
            hljs.highlightBlock(document.getElementById('qa'));
        }
    aside(class="notes").
        Now for the hardest part.  Any questions leveraging promises?
        
